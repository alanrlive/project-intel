# Copy this file to .env.local and edit as needed
# .env.local is gitignored — never commit your actual environment file

# Backend API URL (default: localhost FastAPI)
VITE_API_URL=http://localhost:8000
